@model HoaDon
@{
    ViewData["Title"] = "Chi tiết đơn hàng";
    Layout = "_AdminLayout";
}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2">Chi tiết đơn hàng #@Model.MaHd</h1>
    <a class="btn btn-outline-secondary" asp-action="DonHang">
        <i class="fas fa-arrow-left"></i> Quay lại
    </a>
</div>

<div class="row">
    <div class="col-md-8">
        <div class="card mb-4">
            <div class="card-header">
                <h5><i class="fas fa-list"></i> Sản phẩm đã đặt</h5>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Sản phẩm</th>
                                <th>Đơn giá</th>
                                <th>Số lượng</th>
                                <th>Thành tiền</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.ChiTietHoaDons)
                            {
                                <tr>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <div class="me-3">
                                                <i class="fas fa-coffee fa-2x text-muted"></i>
                                            </div>
                                            <div>
                                                <h6 class="mb-0">@item.MaSpNavigation?.TenSp</h6>
                                                @if (!string.IsNullOrEmpty(item.GhiChu))
                                                {
                                                    <small class="text-muted">@item.GhiChu</small>
                                                }
                                            </div>
                                        </div>
                                    </td>
                                    <td>@item.DonGia.ToString("N0") VNĐ</td>
                                    <td>@item.SoLuong</td>
                                    <td><strong>@item.ThanhTien.ToString("N0") VNĐ</strong></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-4">
        <div class="card mb-3">
            <div class="card-header">
                <h5><i class="fas fa-info-circle"></i> Thông tin đơn hàng</h5>
            </div>
            <div class="card-body">
                <p><strong>Mã đơn hàng:</strong> @Model.MaHd</p>
                <p><strong>Ngày đặt:</strong> @Model.NgayLap?.ToString("dd/MM/yyyy HH:mm")</p>
                <p><strong>Khách hàng:</strong> @Model.MaKhNavigation?.TenKh</p>
                <p><strong>Điện thoại:</strong> @Model.MaKhNavigation?.Sdt</p>
                <p><strong>Địa chỉ:</strong> @Model.MaKhNavigation?.DiaChi</p>
                <p>
                    <strong>Trạng thái:</strong>
                    @{
                        string badgeClass = Model.TrangThaiDonHang switch
                        {
                            "Chờ xác nhận" => "bg-warning",
                            "Đã xác nhận" => "bg-info",
                            "Đang giao" => "bg-primary",
                            "Đã giao" => "bg-success",
                            "Đã hủy" => "bg-danger",
                            _ => "bg-secondary"
                        };
                    }
                    <span class="badge @badgeClass">@Model.TrangThaiDonHang</span>
                </p>
                <p><strong>Thanh toán:</strong> @Model.HinhThucThanhToan</p>
                @if (!string.IsNullOrEmpty(Model.GhiChu))
                {
                    <p><strong>Ghi chú:</strong> @Model.GhiChu</p>
                }
                @if (Model.MaNvNavigation != null)
                {
                    <p><strong>Nhân viên giao:</strong> @Model.MaNvNavigation.TenNv</p>
                }
            </div>
        </div>

        <div class="card mb-3">
            <div class="card-body">
                <div class="d-flex justify-content-between mb-2">
                    <span>Tổng tiền:</span>
                    <span>@Model.TongTien?.ToString("N0") VNĐ</span>
                </div>
                <div class="d-flex justify-content-between mb-2">
                    <span>Giảm giá:</span>
                    <span>-@Model.GiamGia?.ToString("N0") VNĐ</span>
                </div>
                <hr>
                <div class="d-flex justify-content-between">
                    <strong>Thành tiền:</strong>
                    <strong class="text-success">@Model.ThanhTien?.ToString("N0") VNĐ</strong>
                </div>
            </div>
        </div>

        @if (Model.TrangThaiDonHang == "Chờ xác nhận")
        {
            <div class="card">
                <div class="card-body">
                    <h6>Thao tác</h6>
                    <div class="d-grid gap-2">
                        <form method="post" asp-action="UpdateDonHang">
                            <input type="hidden" name="maHD" value="@Model.MaHd">
                            <input type="hidden" name="trangThai" value="Đã xác nhận">
                            <button type="submit" class="btn btn-success w-100 mb-2"
                                    onclick="return confirm('Xác nhận duyệt đơn hàng?')">
                                <i class="fas fa-check"></i> Duyệt đơn
                            </button>
                        </form>
                        <form method="post" asp-action="UpdateDonHang">
                            <input type="hidden" name="maHD" value="@Model.MaHd">
                            <input type="hidden" name="trangThai" value="Đã hủy">
                            <button type="submit" class="btn btn-danger w-100"
                                    onclick="return confirm('Xác nhận hủy đơn hàng?')">
                                <i class="fas fa-times"></i> Hủy đơn
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        }
    </div>
</div>